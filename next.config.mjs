/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "wry-monitor-396.convex.cloud",
        },
      ],
    },
  };
  
  export default nextConfig;
